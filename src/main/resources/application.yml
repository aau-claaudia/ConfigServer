server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: git@github.com:aau-claaudia/config.git
          defaultLabel: main

logging:
  level:
    org:
      springframework:
        cloud:
          config: DEBUG
